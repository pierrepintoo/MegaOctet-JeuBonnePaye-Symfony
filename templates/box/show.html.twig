{% extends 'base.html.twig' %}

{% block title %}Box{% endblock %}

{% block body %}
    <h1>Box</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ box.id }}</td>
            </tr>
            <tr>
                <th>Box_position</th>
                <td>{{ box.boxPosition }}</td>
            </tr>
            <tr>
                <th>Box_heure</th>
                <td>{{ box.boxHeure }}</td>
            </tr>
            <tr>
                <th>Box_image</th>
                <td>{{ box.boxImage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('box_index') }}">back to list</a>

    <a href="{{ path('box_edit', {'id': box.id}) }}">edit</a>

    {{ include('box/_delete_form.html.twig') }}
{% endblock %}
